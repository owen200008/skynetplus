--指明自己的远程id
--serverid=1
--servername="defaultserver"

--日志文件目录
logpath="$GetModulePath/log/"

--启动的网络线程
NetThreadCount=2

--启动工作线程数
workthread=2

--加载的动态库
dlllist="kernel.dll;defaultbussiness.dll"
--替换的动态库
--replacedlllist="skynettest.dll>skynettest_V2.dll"
--默认启动的主模板类
maintemplate="CStartModule"
